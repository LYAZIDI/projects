// Placeholder spec so tsconfig.spec.json has at least one matching file and
// `ng test` has something to run. Replace with real unit tests as they're added.
describe('test runner', () => {
  it('is wired up', () => {
    expect(true).toBe(true);
  });
});
